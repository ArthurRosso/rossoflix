import React from "react";

import "./styles.css"

const Header = () => (
    <nav>
        <div class="nav-wrapper">
            <a href="/" class="brand-logo">Rossoflix</a>
        </div>
    </nav>
);

export default Header;
